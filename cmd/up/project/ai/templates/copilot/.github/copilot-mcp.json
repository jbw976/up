{
  "mcpServers": {
    "upbound-marketplace": {
      "type": "local",
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "-v",
        "{{ .UpConfigDir }}/:/mcp/.up:ro",
        "{{ .MarketPlaceMCP }}"
      ],
      "tools": [
        "*"
      ]
    }
  }
}